---
description: Pointer to the canonical VRodos styling rules
---

# VRodos Styling Guide

Styling rules live in `../../AGENTS.md` under `Styling And Templates`.

This file is kept only as a stable pointer for old workflow references. Do not add independent styling policy here; update `AGENTS.md` instead.
