# VRodos Gemini Notes

This tool-specific file is intentionally minimal.

Use `../AGENTS.md` as the canonical source for VRodos architecture, styling, rendering, build, and cleanup rules. The older Gemini-specific styling notes were removed because they contradicted the current Tailwind/DaisyUI policy in `AGENTS.md`.
