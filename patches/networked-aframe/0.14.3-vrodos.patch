diff --git a/src/NetworkConnection.js b/src/NetworkConnection.js
index 99ae7b5..9a89c19 100644
--- a/src/NetworkConnection.js
+++ b/src/NetworkConnection.js
@@ -99,10 +99,16 @@ class NetworkConnection {
   // Some adapters will handle this internally
   checkForConnectingClients(occupantList) {
     for (var id in occupantList) {
-      var startConnection = this.isNewClient(id) && this.adapter.shouldStartConnectionTo(occupantList[id]);
-      if (startConnection) {
-        NAF.log.write('Opening datachannel to', id);
-        this.adapter.startStreamConnection(id);
+      if (this.isNewClient(id)) {
+        var startConnection = this.adapter.shouldStartConnectionTo(occupantList[id], id);
+        if (startConnection) {
+          NAF.log.write('Opening datachannel to', id);
+          this.adapter.startStreamConnection(id);
+        }
+
+        // Sync state via WS immediately so newcomers see us even if P2P lags/fails
+        NAF.log.write('Forcing early sync to new client via WS', id);
+        this.entities.completeSync(id, true);
       }
     }
   }
diff --git a/src/adapters/EasyRtcAdapter.js b/src/adapters/EasyRtcAdapter.js
index 5b6af7b..a9253cb 100644
--- a/src/adapters/EasyRtcAdapter.js
+++ b/src/adapters/EasyRtcAdapter.js
@@ -69,10 +69,8 @@ class EasyRtcAdapter extends NoOpAdapter {
   }
 
   setRoomOccupantListener(occupantListener) {
-    this.easyrtc.setRoomOccupantListener((roomName, occupants, primary) => {
-      if (roomName === this.room) {
-        occupantListener(occupants);
-      }
+    this.easyrtc.setRoomOccupantListener(function (roomName, occupants, primary) {
+      occupantListener(occupants);
     });
   }
 
@@ -120,15 +118,17 @@ class EasyRtcAdapter extends NoOpAdapter {
       this.easyrtc.joinRoom(this.room, null,
         () => {
+          this._myRoomJoinTime = this._getRoomJoinTime(clientId);
           this.connectSuccess(clientId);
         },
         (errorCode, errorText) => { this.connectFailure(errorCode, errorText); }
       );
     }).catch(this.connectFailure);
   }
 
-  shouldStartConnectionTo(client) {
-    if (this._myRoomJoinTime === undefined) {
-      this._myRoomJoinTime = this._getRoomJoinTime(this.easyrtc.myEasyrtcid);
+  shouldStartConnectionTo(client, clientId) {
+    const remoteId = client.id || clientId;
+    if (this._myRoomJoinTime === client.roomJoinTime && remoteId) {
+      return this.easyrtc.myEasyrtcid < remoteId;
     }
     return this._myRoomJoinTime <= client.roomJoinTime;
   }
@@ -144,8 +149,14 @@ class EasyRtcAdapter extends NoOpAdapter {
           NAF.log.write("Successfully started datachannel to ", caller);
         }
       },
       function(errorCode, errorText) {
-        NAF.log.error(errorCode, errorText);
+        if (errorCode === 'ALREADY_CONNECTED') {
+          // This is a harmless race condition when both clients try to connect simultaneously.
+          // EasyRTC will drop the duplicate connection attempt gracefully.
+          NAF.log.write('Ignoring ALREADY_CONNECTED glare from ' + clientId);
+        } else {
+          NAF.log.error(errorCode, errorText);
+        }
       },
       function(wasAccepted) {
         // console.log("was accepted=" + wasAccepted);
@@ -157,7 +168,9 @@ class EasyRtcAdapter extends NoOpAdapter {
 
   sendData(clientId, dataType, data) {
     // send via webrtc otherwise fallback to websockets
-    this.easyrtc.sendData(clientId, dataType, data);
+    // Clone data to avoid closure races if tick modifies it before async send finishes
+    const msgData = typeof data === 'object' && data !== null ? JSON.parse(JSON.stringify(data)) : data;
+    this.easyrtc.sendData(clientId, dataType, msgData);
   }
 
   sendDataGuaranteed(clientId, dataType, data) {
@@ -167,15 +180,15 @@ class EasyRtcAdapter extends NoOpAdapter {
   broadcastData(dataType, data) {
     var roomOccupants = this.easyrtc.getRoomOccupantsAsMap(this.room);
 
+    // Clone data once per broadcast to avoid closure races
+    const msgData = typeof data === 'object' && data !== null ? JSON.parse(JSON.stringify(data)) : data;
+
     // Iterate over the keys of the easyrtc room occupants map.
     // getRoomOccupantsAsArray uses Object.keys which allocates memory.
     for (var roomOccupant in roomOccupants) {
-      if (
-        roomOccupants[roomOccupant] &&
-        roomOccupant !== this.easyrtc.myEasyrtcid
-      ) {
+      if (roomOccupants[roomOccupant] && roomOccupant !== this.easyrtc.myEasyrtcid) {
         // send via webrtc otherwise fallback to websockets
-        this.easyrtc.sendData(roomOccupant, dataType, data);
+        this.easyrtc.sendData(roomOccupant, dataType, msgData);
       }
     }
   }
@@ -339,9 +352,9 @@ class EasyRtcAdapter extends NoOpAdapter {
   }
 
   _getRoomJoinTime(clientId) {
-    var myRoomId = NAF.room;
-    var joinTime = this.easyrtc.getRoomOccupantsAsMap(myRoomId)[clientId]
-      .roomJoinTime;
+    var myRoomId = this.room;
+    var occupants = this.easyrtc.getRoomOccupantsAsMap(myRoomId) || {};
+    var joinTime = occupants[clientId] ? occupants[clientId].roomJoinTime : Date.now();
     return joinTime;
   }
 
diff --git a/package.json b/package.json
index 092d253..045439b 100644
--- a/package.json
+++ b/package.json
@@ -27,7 +27,7 @@
   "dependencies": {
     "buffered-interpolation": "Infinitelee/buffered-interpolation#5bb18421ebf2bf11664645cdc7a15bd77ee2156b",
-    "express": "^4.17.3",
+    "express": "^4.22.1",
     "open-easyrtc": "^2.1.8",
-    "socket.io": "^4.8.1"
+    "socket.io": "^4.8.3"
   },
   "devDependencies": {
@@ -64,5 +64,10 @@
     "webpack-cli": "^6.0.0",
     "webpack-dev-middleware": "^7.1.1",
     "webpack-dev-server": "^5.1.0"
+  },
+  "overrides": {
+    "diff": "^8.0.3",
+    "got": "^11.8.6",
+    "serialize-javascript": "^7.0.3"
   },
   "lint-staged": {
